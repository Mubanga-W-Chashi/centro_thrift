import { FoundationPage } from "@/components/layout/foundation-page";

export default function CartPage() {
  return (
    <FoundationPage
      description="The cart route is ready for delivery-day selection and WhatsApp reservation handoff."
      eyebrow="Reservation flow"
      title="Cart"
    />
  );
}
