import { FoundationPage } from "@/components/layout/foundation-page";

export default function AdminDashboardPage() {
  return (
    <FoundationPage
      description="The admin dashboard route is ready for metrics, activity, products, reservations, orders, deliveries, and customer modules."
      eyebrow="Admin"
      title="Dashboard"
    />
  );
}
