import { FoundationPage } from "@/components/layout/foundation-page";

export default function AdminLoginPage() {
  return (
    <FoundationPage
      description="The staff login route is connected to the Auth.js credentials provider and ready for the Sprint 2 role system."
      eyebrow="Staff access"
      title="Login"
    />
  );
}
