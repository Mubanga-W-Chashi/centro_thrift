import { FoundationPage } from "@/components/layout/foundation-page";

export default function AdminOrdersPage() {
  return (
    <FoundationPage
      description="The orders admin route is ready for payment-confirmed order workflows and SOLD inventory transitions."
      eyebrow="Admin"
      title="Orders"
    />
  );
}
