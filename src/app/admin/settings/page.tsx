import { FoundationPage } from "@/components/layout/foundation-page";

export default function AdminSettingsPage() {
  return (
    <FoundationPage
      description="The settings route is reserved for Admin-only platform controls in the role system."
      eyebrow="Admin"
      title="Settings"
    />
  );
}
