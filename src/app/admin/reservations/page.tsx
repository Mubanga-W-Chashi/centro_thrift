import { FoundationPage } from "@/components/layout/foundation-page";

export default function AdminReservationsPage() {
  return (
    <FoundationPage
      description="The reservations admin route is ready for active, expired, paid, and cancelled reservation workflows."
      eyebrow="Admin"
      title="Reservations"
    />
  );
}
